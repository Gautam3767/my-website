name = "my-website"
compatibility_date = "2026-05-18"

# Serve the Vite build output as static assets.
# `single-page-application` ensures /writing, /admin, etc. all fall back
# to index.html so React Router can pick up the route client-side.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
